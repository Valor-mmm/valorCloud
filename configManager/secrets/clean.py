from typing import List

from ..parseServerConfig import Server
from .handlers import SecretHandler


def clean(target: str, handler: SecretHandler, server_config: List[Server], root_directory: str, debug: bool) -> None:
    pass
